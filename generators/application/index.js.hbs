
namespace `{{FQNS_to_NS namespace}}` (
    class {{FQNS_to_Class namespace}} extends Application {
        constructor(element){
            super(element);
        }

        async onConnected() {
            await super.onConnected();
            alert("onConnected")
        }
    }
);
